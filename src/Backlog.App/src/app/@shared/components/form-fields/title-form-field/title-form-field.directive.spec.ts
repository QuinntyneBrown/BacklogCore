import { TitleFormFieldDirective } from './title-form-field.directive';

describe('TitleFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
