import { PillDirective } from './pill.directive';

describe('PillDirective', () => {
  it('should create an instance', () => {
    const directive = new PillDirective();
    expect(directive).toBeTruthy();
  });
});
