import { SubHeadingDirective } from './sub-heading.directive';

describe('SubHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new SubHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
